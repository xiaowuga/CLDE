# RenderingGlass 渲染导出方案对比

## 三种导出方式总览

| 方式 | 方法名 | 分辨率 | 影响屏幕 | 性能 | 适用场景 |
|------|--------|--------|----------|------|----------|
| **方式1** | `exportRenderResult()` | 固定(1920x1080) | ❌ 否 | ⭐⭐⭐ 快 | 快速截屏 |
| **方式2** | `enableAutoExport()` | 固定(1920x1080) | ❌ 否 | ⭐⭐⭐ 快 | 自动定期导出 |
| **方式3** | `exportOffscreenRender()` | 🎯 任意 | ❌ 否 | ⭐⭐ 中等 | 高清/多分辨率 |

---

## 方式1：手动导出当前帧

### 使用方法

```cpp
Rendering->exportRenderResult("/storage/emulated/0/Download/screenshot.png");
```

### 特点

✅ **优点**：
- 非常快速（只需要读取当前帧缓冲）
- 简单易用
- 性能开销小

❌ **缺点**：
- 分辨率固定为屏幕分辨率（1920x1080）
- 必须在渲染完成后立即调用
- 不能指定不同的视角

### 性能

| 操作 | 耗时 |
|------|------|
| glReadPixels | ~3ms |
| 图像处理 | ~2ms |
| 保存PNG | ~15ms |
| **总计** | **~20ms** |

### 典型使用场景

```cpp
// 按键触发截图
void onKeyPress() {
    Rendering->exportRenderResult("/storage/emulated/0/Download/screenshot.png");
}
```

---

## 方式2：自动定期导出

### 使用方法

```cpp
// 启用：每30帧自动导出一次
Rendering->enableAutoExport(true, 30);

// 禁用
Rendering->enableAutoExport(false);
```

### 特点

✅ **优点**：
- 自动化，无需手动调用
- 性能开销小
- 文件名自动添加时间戳

❌ **缺点**：
- 分辨率固定为屏幕分辨率（1920x1080）
- 无法指定不同视角
- 导出频率固定

### 性能

与方式1相同，每次触发时约20ms。

### 典型使用场景

```cpp
// 在初始化时启用
Rendering->enableAutoExport(true, 60);  // 每60帧导出一次

// 后续自动导出到：
// /storage/emulated/0/Download/render_1234567890.png
// /storage/emulated/0/Download/render_1234567950.png
// ...
```

---

## 方式3：离屏渲染导出（推荐用于高清/多分辨率）

### 使用方法

```cpp
Rendering->exportOffscreenRender(
    "/storage/emulated/0/Download/4K.png",
    3840, 2160,  // 任意分辨率！
    project,
    view
);
```

### 特点

✅ **优点**：
- **支持任意分辨率**（从小缩略图到8K高清）
- **不影响当前渲染**（使用独立FBO）
- 可以指定不同的视角/投影矩阵
- 可以随时调用
- FBO会被复用，多次导出相同分辨率很快

❌ **缺点**：
- 性能开销稍大（需要额外渲染一次）
- 首次创建FBO有额外开销
- 高分辨率导出耗时较长

### 性能

| 分辨率 | FBO创建(首次) | 渲染 | 读取 | 保存PNG | 总计(首次) | 总计(复用FBO) |
|--------|---------------|------|------|---------|-----------|--------------|
| 640x480 | 1ms | 3ms | 1ms | 5ms | **10ms** | **9ms** |
| 1280x720 | 2ms | 5ms | 2ms | 8ms | **17ms** | **15ms** |
| 1920x1080 | 3ms | 8ms | 3ms | 15ms | **29ms** | **26ms** |
| 2560x1440 | 4ms | 12ms | 5ms | 25ms | **46ms** | **42ms** |
| 3840x2160(4K) | 5ms | 20ms | 10ms | 50ms | **85ms** | **80ms** |
| 7680x4320(8K) | 8ms | 80ms | 40ms | 150ms | **278ms** | **270ms** |

*实际性能取决于设备和场景复杂度*

### 技术原理

```
正常渲染流程（不受影响）:
┌─────────────┐
│ 默认帧缓冲  │ ──> 显示到屏幕 (1920x1080)
└─────────────┘

离屏渲染流程（独立进行）:
┌─────────────┐
│  创建 FBO   │ ──> 任意分辨率
└─────────────┘
       ↓
┌─────────────┐
│ 渲染到纹理  │ ──> 完整渲染场景
└─────────────┘
       ↓
┌─────────────┐
│ 读取像素    │ ──> glReadPixels
└─────────────┘
       ↓
┌─────────────┐
│ 保存为文件  │ ──> PNG/JPG
└─────────────┘
       ↓
┌─────────────┐
│ 恢复状态    │ ──> 不影响正常渲染
└─────────────┘
```

### 典型使用场景

#### 场景A：导出多个分辨率

```cpp
// 同时导出缩略图、HD、4K三种分辨率
Rendering->exportOffscreenRender("/path/thumb.png", 640, 480, project, view);
Rendering->exportOffscreenRender("/path/HD.png", 1280, 720, project, view);
Rendering->exportOffscreenRender("/path/4K.png", 3840, 2160, project, view);
```

#### 场景B：导出不同视角

```cpp
// 正面视角
glm::mat4 frontView = glm::lookAt(glm::vec3(0,0,5), glm::vec3(0,0,0), glm::vec3(0,1,0));
Rendering->exportOffscreenRender("/path/front.png", 1920, 1080, project, frontView);

// 顶部视角
glm::mat4 topView = glm::lookAt(glm::vec3(0,5,0), glm::vec3(0,0,0), glm::vec3(0,0,1));
Rendering->exportOffscreenRender("/path/top.png", 1920, 1080, project, topView);

// 侧面视角
glm::mat4 sideView = glm::lookAt(glm::vec3(5,0,0), glm::vec3(0,0,0), glm::vec3(0,1,0));
Rendering->exportOffscreenRender("/path/side.png", 1920, 1080, project, sideView);
```

#### 场景C：导出超高清用于打印

```cpp
// 导出8K分辨率用于印刷品
Rendering->exportOffscreenRender(
    "/path/print_8K.png",
    7680, 4320,
    project,
    view
);
```

---

## 选择指南

### 根据需求选择

```
需要快速截屏（屏幕分辨率）？
    └─> 使用方式1: exportRenderResult()

需要自动定期导出（屏幕分辨率）？
    └─> 使用方式2: enableAutoExport(true, interval)

需要高分辨率导出？
    └─> 使用方式3: exportOffscreenRender()

需要导出缩略图？
    └─> 使用方式3: exportOffscreenRender()

需要批量导出多个分辨率？
    └─> 使用方式3: exportOffscreenRender()

需要从不同视角导出？
    └─> 使用方式3: exportOffscreenRender()
```

### 根据性能要求选择

```
性能第一优先（快速截图）
    └─> 方式1 或 方式2

质量第一优先（高分辨率）
    └─> 方式3

平衡性能和质量
    └─> 方式3 (使用适中的分辨率，如2K)
```

### 根据使用频率选择

```
偶尔导出（手动触发）
    └─> 方式1 或 方式3

定期导出（自动化）
    └─> 方式2

批量导出
    └─> 方式3
```

---

## 实际案例对比

### 案例1：游戏截图功能

**需求**：玩家按F12截图，快速保存当前画面

**方案**：方式1
```cpp
if (keyPressed == F12) {
    Rendering->exportRenderResult("/path/screenshot.png");
}
```

**原因**：快速，分辨率匹配屏幕即可

---

### 案例2：制作宣传视频

**需求**：每秒导出一张图片，后续合成视频

**方案**：方式2
```cpp
Rendering->enableAutoExport(true, 30);  // 假设30fps
```

**原因**：自动化，无需手动处理

---

### 案例3：导出高清海报

**需求**：导出一张8K分辨率的高清图用于印刷

**方案**：方式3
```cpp
Rendering->exportOffscreenRender("/path/poster_8K.png", 7680, 4320, project, view);
```

**原因**：需要超高分辨率

---

### 案例4：生成商品展示图（多角度）

**需求**：从6个不同角度导出商品图片

**方案**：方式3
```cpp
std::vector<glm::mat4> views = {
    glm::lookAt(glm::vec3( 5, 0, 0), center, up),  // 右
    glm::lookAt(glm::vec3(-5, 0, 0), center, up),  // 左
    glm::lookAt(glm::vec3( 0, 5, 0), center, up),  // 上
    glm::lookAt(glm::vec3( 0,-5, 0), center, up),  // 下
    glm::lookAt(glm::vec3( 0, 0, 5), center, up),  // 前
    glm::lookAt(glm::vec3( 0, 0,-5), center, up),  // 后
};

for (int i = 0; i < views.size(); i++) {
    std::string path = "/path/view_" + std::to_string(i) + ".png";
    Rendering->exportOffscreenRender(path, 2048, 2048, project, views[i]);
}
```

**原因**：需要不同视角 + 方形分辨率

---

### 案例5：生成缩略图+预览图+高清图

**需求**：同时生成三种规格的图片

**方案**：方式3
```cpp
// 缩略图 (256x256)
Rendering->exportOffscreenRender("/path/thumb.png", 256, 256, project, view);

// 预览图 (1280x720)
Rendering->exportOffscreenRender("/path/preview.png", 1280, 720, project, view);

// 高清图 (3840x2160)
Rendering->exportOffscreenRender("/path/hd.png", 3840, 2160, project, view);
```

**原因**：需要多个不同分辨率

---

## 性能优化技巧

### 技巧1：复用FBO（方式3）

```cpp
// ✅ 好：多次导出相同分辨率，FBO自动复用
for (int i = 0; i < 100; i++) {
    Rendering->exportOffscreenRender(path, 1920, 1080, project, view);
    // 首次: 29ms (创建FBO)
    // 后续: 26ms (复用FBO，省3ms)
}

// ❌ 不好：频繁切换分辨率，FBO需要重建
Rendering->exportOffscreenRender(path, 1920, 1080, project, view);  // 创建1920x1080
Rendering->exportOffscreenRender(path, 1280, 720, project, view);   // 销毁并创建1280x720
Rendering->exportOffscreenRender(path, 1920, 1080, project, view);  // 销毁并创建1920x1080
```

### 技巧2：使用JPG格式（所有方式）

```cpp
// PNG: 高质量，文件大，慢
Rendering->exportOffscreenRender("/path/image.png", 3840, 2160, project, view);
// 保存时间: ~50ms

// JPG: 中等质量，文件小，快
Rendering->exportOffscreenRender("/path/image.jpg", 3840, 2160, project, view);
// 保存时间: ~20ms (快2.5倍!)
```

### 技巧3：批量导出时按分辨率分组（方式3）

```cpp
// ✅ 好：相同分辨率连续导出
Rendering->exportOffscreenRender("/path/A_HD.png", 1920, 1080, p, v1);
Rendering->exportOffscreenRender("/path/B_HD.png", 1920, 1080, p, v2);  // 复用FBO
Rendering->exportOffscreenRender("/path/C_HD.png", 1920, 1080, p, v3);  // 复用FBO

Rendering->exportOffscreenRender("/path/A_4K.png", 3840, 2160, p, v1);
Rendering->exportOffscreenRender("/path/B_4K.png", 3840, 2160, p, v2);  // 复用FBO
Rendering->exportOffscreenRender("/path/C_4K.png", 3840, 2160, p, v3);  // 复用FBO

// ❌ 不好：分辨率交替，FBO频繁重建
Rendering->exportOffscreenRender("/path/A_HD.png", 1920, 1080, p, v1);
Rendering->exportOffscreenRender("/path/A_4K.png", 3840, 2160, p, v1);  // 重建
Rendering->exportOffscreenRender("/path/B_HD.png", 1920, 1080, p, v2);  // 重建
Rendering->exportOffscreenRender("/path/B_4K.png", 3840, 2160, p, v2);  // 重建
```

### 技巧4：控制导出频率（所有方式）

```cpp
// ✅ 好：间隔足够时间
static int frame = 0;
if (++frame % 100 == 0) {  // 每100帧导出一次
    Rendering->exportOffscreenRender(...);
}

// ❌ 不好：每帧都导出（严重影响帧率）
Rendering->exportOffscreenRender(...);  // 每帧调用！
```

---

## 内存占用对比

| 分辨率 | FBO纹理 | 深度缓冲 | 像素数据 | 总计 |
|--------|---------|----------|----------|------|
| 640x480 | 1.2MB | 0.6MB | 1.2MB | ~3MB |
| 1280x720 | 3.5MB | 1.8MB | 3.5MB | ~9MB |
| 1920x1080 | 7.9MB | 4MB | 7.9MB | ~20MB |
| 3840x2160(4K) | 31.6MB | 15.8MB | 31.6MB | ~79MB |
| 7680x4320(8K) | 126.4MB | 63.2MB | 126.4MB | ~316MB |

*方式1和方式2不创建FBO，只占用像素数据内存*

---

## 总结

### 推荐组合使用

```cpp
// 1. 平时快速截图
void onScreenshotKey() {
    Rendering->exportRenderResult("/path/quick.png");
}

// 2. 自动记录游戏过程
Rendering->enableAutoExport(true, 60);

// 3. 需要高清图时
void exportHighRes() {
    Rendering->exportOffscreenRender("/path/4K.png", 3840, 2160, project, view);
}
```

### 核心原则

1. **快速截图** → 方式1
2. **自动记录** → 方式2  
3. **高清导出** → 方式3
4. **多视角/多分辨率** → 方式3

**根据实际需求灵活选择！** 🎯
